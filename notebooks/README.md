# Notebooks

Notebooks are reserved for exploration and presentation. Reusable transformations, metrics, models, and plotting logic must live under `src/` and be covered by tests.

Notebooks must not contain secrets, private prompts, or embedded raw benchmark data.
